horizontal = true
fileby = "annis::doc"  # anno keys can be specified as strings and as map, so here we test both, see also `anno` below
groupby = { ns = "", name = "sentence" }
group_component_type = "Coverage"
component = { ctype = "Ordering", layer = "annis", name = "norm" }
anno = { ns = "", name = "norm" }
