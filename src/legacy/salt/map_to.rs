use crate::error::Result;
use graphannis::AnnotationGraph;
use j4rs::{Instance, Jvm};

pub fn map_document_graph(g: &AnnotationGraph, document_id: &str, jvm: &Jvm) -> Result<Instance> {
    todo!()
}
